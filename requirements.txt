# Core ML / CV
ultralytics==8.2.*       # YOLOv8 training & export
opencv-python==4.5.*     # use opencv-python on dev machine; CUDA-enabled OpenCV ships with JetPack on-device
numpy<2.0
onnx
onnxruntime

# Jetson-specific (install via JetPack / NVIDIA wheels on-device, not pip on dev machine)
# Jetson.GPIO
# pycuda
# tensorrt   # comes pre-installed with JetPack — do not pip install on Jetson

# System / comms
paho-mqtt==1.6.*
pyyaml

# Logging / data
pandas

# Dev tooling
black
flake8
pytest
